# README Quick Start Audit (phase 15, 2026-09-23)

Verdict: already present, line 11-20, skip.

`README.md` line 11 opens with `## Run the workbench locally`, which is the
equivalent of a "Quick start" section. It names the Node 24 requirement,
runs `npm ci --ignore-scripts` then `npm run dev`, points at the loopback URL
`http://127.0.0.1:4173`, and mentions `npm run build` followed by
`npm run preview` for the verified hashed build. Three concrete commands in
code block; nothing to add.
